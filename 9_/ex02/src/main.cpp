/*
void	printNumbers() {
	std::cout << "Stored values: ";
	for (std::set<int>::iterator it = _storage.begin(); it != _storage.end(); it++)
		std::cout << *it << " ";
	std::cout << std::endl;	
}*/

#include "../include/PmergeMe.hpp"

int main(int ac, char ** av) {
    std::cout << OLIVE "PROGRAM STARTED" RESET << std::endl;

    (void)ac;
    (void)av;

    return 0;
}